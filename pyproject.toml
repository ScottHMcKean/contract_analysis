[project]
name = "contract-analysis"
version = "0.1.0"
description = "Contract analysis pipeline using generative AI on Databricks"
requires-python = ">=3.10"
dependencies = [
    "playwright>=1.58.0",
]

[project.optional-dependencies]
dev = [
    "streamlit",
    "plotly",
    "pandas",
]
